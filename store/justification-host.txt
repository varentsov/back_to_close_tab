A content script must run on pages to notice a back gesture — a mouse back button press, or ⌘[ / ⌘← / Alt+←. It inspects only the button number and key of those events. It reads no page content, no form data and no URLs, and makes no network requests. Back gestures can happen on any site the user visits, so the script cannot be limited to a fixed list of hosts.
